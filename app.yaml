command:
  - uvicorn
  - app.main:app
  - --host
  - 0.0.0.0
  - --port
  - "$DATABRICKS_APP_PORT"
env:
  - name: DATABRICKS_SQL_WAREHOUSE_ID
    value: "REPLACE_WITH_YOUR_WAREHOUSE_ID"
  - name: DIRECT_SQL_TIMEOUT_SECONDS
    value: "45"
