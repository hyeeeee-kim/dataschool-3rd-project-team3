command:
  - uvicorn
  - app.main:app
  - --host
  - "0.0.0.0"
  - --port
  - "8000"

env:
  - name: DATABRICKS_WAREHOUSE_ID
    valueFrom: sql_warehouse
  - name: ADMIN_USERNAME
    value: "admin"
  - name: ADMIN_PASSWORD
    valueFrom: admin_password
